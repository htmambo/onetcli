## 审查报告（terminal-ligatures-setting）
生成时间：2026-03-26 21:16:53 CST

### 需求完整性检查
- 目标明确：在首页设置中新增终端连字开关，用于控制终端里编程字体的符号连字效果
- 范围明确：仅涉及首页设置、全局终端设置同步与终端渲染链路；按用户要求不涉及终端右侧设置面板
- 交付物明确：设置字段、首页设置入口、终端渲染开关、本地验证、`.claude/` 留痕
- 风险与依赖明确：连字会影响字体宽度，因此文本绘制和字宽测量必须共用同一布尔值

### 技术维度评分
- 代码质量：95/100
- 测试覆盖：86/100
- 规范遵循：95/100

### 战略维度评分
- 需求匹配：98/100
- 架构一致：97/100
- 风险评估：95/100

### 综合评分
- 95/100
- 建议：通过

### 结论
- 首页入口已补齐：`main/src/setting_tab.rs` 现在把 `terminal_font_ligatures` 纳入 `AppSettings`，并在“终端”分组新增开关，修改后直接广播到所有终端实例。
- 同步链路保持一致：`main/src/home/home_tabs.rs` 在终端初始化和批量更新时都会把连字开关一起下发，没有另造新的终端状态系统。
- 渲染与测量已统一：`crates/terminal_view/src/view.rs` 的字宽测量和 `crates/terminal_view/src/terminal_element.rs` 的真实文本绘制现在共用 `terminal_font_features(...)`，避免只改一半导致列宽、光标或选区错位。
- 默认行为稳定：新设置默认关闭，因此老用户不主动开启时，终端表现不会突变。
- 本地验证有效：`cargo fmt --all` 与 `cargo check -p terminal_view -p main` 均通过；残余风险主要是当前没有 GUI 自动化，只能靠手工切换字体确认视觉效果。
