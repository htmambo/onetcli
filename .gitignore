/target
.DS_Store
/docks.json
/profile.json
.vscode
index.scip
*.log
node_modules/

.worktrees/

# proptest 自动生成的反例回归目录（失败测试的反例，下次跑会重放）
# 不应入仓：每次失败都覆盖，体积小但语义是临时反例池
**/proptest-regressions/

# Rust build artifacts
target/
**/target/
crates/**/target/

# Debug artifacts
**/*.rs.bk
*.pdb

# IDEs
.idea/
.idea/vcs.xml
.vscode/
*.swp
*.swo
*~
.zed/

# Cargo lock for libraries (keep for binaries)
# Cargo.lock is already tracked, so commenting this out
# Cargo.lock

# Backup files
*.bak
*.tmp

# OS specific
Thumbs.db
.Spotlight-V100
.Trashes

# Windows special device names
nul

# Environment variables
.env
.env.local
.env.*.local

# Local-only scripts
/supabase.sql

# AI Coding 生成的提交文档
commit-info.md

# macOS 应用图标（二进制资源，单独提交）
resources/macos/OnetCli.icns

# vibe coding
.worktrees/
.omc/
.claude/
.ace-tool/
.superpowers/
.codex
